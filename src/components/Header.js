const Header = ({headding,subheadding}) => {
    return (
        <div className='container'>
            <h1>{headding}</h1>
            <h6>{subheadding}</h6>
        </div>
    )
}

export default Header
